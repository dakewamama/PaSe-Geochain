import { ChainType } from './types/chain';
export declare const GKN_SERVICE_ENDPOINTS: Record<string, string>;
export type GknData = {
    id: string;
    chains: Partial<Record<ChainType, GknDataChain>>;
    client?: {
        tokenActiveDisplay?: 'default' | 'minimal' | 'hidden';
    };
};
export type GknDataChain = {
    frontendPollingTimeoutSeconds?: number;
    chainSpecificId?: string;
};
export declare const getGatekeeperNetworkData: (stage: string, gknId: string) => Promise<GknData>;
