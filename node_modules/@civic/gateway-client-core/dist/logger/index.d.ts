import { LoggingInterface } from './provider.logger';
declare const localLogger: LoggingInterface;
export declare const prefixLogger: (prefix: string, globalPrefix?: string) => LoggingInterface;
export default localLogger;
