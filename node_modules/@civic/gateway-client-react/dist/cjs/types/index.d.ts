import { FlowParameters, FlowStatus, GatewayToken, PendingPayload, UIOutput, UserInteraction } from '@civic/gateway-client-core';
import { GatewayStatus } from './gateway';
export * from './gateway';
export * from './log';
export type ReactClientCoreOutput = {
    gatewayStatus: GatewayStatus;
    gatewayToken?: GatewayToken;
    flowParameters?: FlowParameters;
    gatewayTokenTransaction?: string;
    pendingRequests?: PendingPayload;
    flowState?: {
        status?: FlowStatus;
        userInteraction: UserInteraction;
    };
    ui?: UIOutput;
};
