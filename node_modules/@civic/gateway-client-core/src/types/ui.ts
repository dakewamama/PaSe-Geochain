export type UIInput = {
  onShow: () => void;
  onHide: () => void;
  onLoad: () => void;
};

export type UIOutput = {
  isVisible: boolean;
  url?: string;
  isLoading: boolean;
  isLoaded: boolean;
};
