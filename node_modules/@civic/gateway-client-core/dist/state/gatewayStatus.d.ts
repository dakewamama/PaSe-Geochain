import { InternalGatewayStatus } from '../types/gateway';
import { GatewayCoreState, Transition } from '../types/gatewayCore';
export declare const transitions: Record<Partial<InternalGatewayStatus>, Transition<any, any>>;
export declare const computeInternalStatus: (state: GatewayCoreState) => InternalGatewayStatus;
