import { GatewayToken } from '../types/chain';
export declare const hasExpired: (expiryTime: number) => boolean;
export declare const isTokenRefreshRequired: ({ gatewayToken, tokenExpirationMarginSeconds, forceRequireRefresh, }: {
    gatewayToken?: GatewayToken;
    tokenExpirationMarginSeconds: number;
    forceRequireRefresh?: boolean;
}) => boolean;
export declare const MAX_ALLOWED_TIMEOUT = 2147483647;
export declare const nowSeconds: () => number;
export declare const getTokenRefreshIntervalMilliseconds: (expiryTimeSeconds: number, tokenExpirationMarginSeconds: number) => number;
